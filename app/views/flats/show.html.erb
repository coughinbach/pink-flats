<%= render "shared/navbar" %>
<div class="banner-show" style="background-image: url(<%= cl_image_path @flat.photo, height: 300, width: 400, class: "show-image" %>)">
  <div class="banner-content">
    <a class="btn btn-primary btn-lg">See more pictures</a>
  </div>
</div>

<div class="flexbox">
  <div class="container">
    <div class="row">
      <div class="col-md-5 col-md-offset-1">
        <div class="anchor">
          <a href="#title-show"><p>Presentation</p></a>
          <p>-</p>
          <a href="#reviews-show"><p>Reviews</p></a>
          <p>-</p>
          <a href="#localisation-show"><p>Localisation</p></a>
        </div>
        <div class="container-title-show" id="title-show">
          <h1 class="text-center"><%= @flat.name %></h1>
          <img src="https://kitt.lewagon.com/placeholder/users/adrienmoison" id="avatar-show">
            <%= "usersame" %>
        </div>
        <h3 class="text-center"><%= @flat.address %></h3>
                <% 10.times do %>
                <p id="flat-description"><%= @flat.description %></p>
                <% end %>
        <h3 class="text-center" id="reviews-show"><%= "Reviews" %></h3>
        <p>Maxxxxxx de reviews</p>
        <h3 class="text-center" id="localisation-show"><%= "Localisation" %></h3>
          <script src='https://maps.googleapis.com/maps/api/js?v=3.exp'></script><div style='overflow:hidden;height:440px;width:520px;'><div id='gmap_canvas' style='height:440px;width:520px;'></div><div><small><a href="https://embedgooglemaps.com/fr/">emg.com/fr</a></small></div><div><small><a href="http://botonmegusta.org/">tout site web</a></small></div><style>#gmap_canvas img{max-width:none!important;background:none!important}</style></div><script type='text/javascript'>function init_map(){var myOptions = {zoom:10,center:new google.maps.LatLng(48.856614,2.3522219000000177),mapTypeId: google.maps.MapTypeId.ROADMAP};map = new google.maps.Map(document.getElementById('gmap_canvas'), myOptions);marker = new google.maps.Marker({map: map,position: new google.maps.LatLng(48.856614,2.3522219000000177)});infowindow = new google.maps.InfoWindow({content:'<strong>Villa Gaudelet</strong><br>Paris, France<br>'});google.maps.event.addListener(marker, 'click', function(){infowindow.open(map,marker);});infowindow.open(map,marker);}google.maps.event.addDomListener(window, 'load', init_map);</script>
      </div>
      <div class="col-md-4 col-md-offset-1" id="form-show">
        <h1 class="text-center"><%= @flat.price %>€ per night</h1>
        <% if policy(Booking).create? %>
          <%= render 'new_booking_form' %>
        <% end %>
      </div>
    </div>
  </div>
</div>

<div><%= link_to "Back to list", flats_path, class: "btn btn-primary" %></div>
<% if policy(@flat).update? || current_user.admin %>
    <%= link_to "Edit your flat", edit_flat_path(@flat), class: "btn btn-primary" %>
<%end%>

<% if policy(@flat).destroy? || current_user.admin %>
  <%= link_to "Delete your flat", flat_path(@flat), method: :delete, data: {confirm: "Are you sure?"}, class: "btn btn-primary" %>
<%end %>
