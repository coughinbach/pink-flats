<%= simple_form_for([ flat, review ], remote: true) do |form| %>
  <%= form.input :content, as: :text %>
  <%= form.input :rating, as: :radio_buttons, collection: 1..5 %>
  <%= form.button :submit %>
<% end %>
